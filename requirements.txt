# SorthaDevKit Requirements
# Core dependencies for the enhanced AIF completion with architecture diagram generation

# Existing dependencies (assuming these are already installed)
pandas>=1.5.0
openpyxl>=3.1.0
pydantic>=2.0.0
langchain-openai>=0.1.0

# Additional dependencies for architecture diagram generation
# (These may already be available in Python standard library)
# xml.etree.ElementTree - part of standard library
# json - part of standard library
# datetime - part of standard library
# typing - part of standard library

# Optional: For enhanced diagram features (uncomment if needed)
# Pillow>=9.0.0  # For image processing
# matplotlib>=3.5.0  # For advanced plotting
# networkx>=2.8.0  # For graph-based layouts
